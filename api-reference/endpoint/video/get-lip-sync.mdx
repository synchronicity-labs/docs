---
openapi: get /video/{id}
deprecated: true
---

# introduction

we have deprecated the `/video/{id}` endpoint in favor of the our new `/lipsync/{id}` endpoint.

<CardGroup cols={1}>
  <Card title="/lipsync/{id}" icon="rotate" href="/api-reference/endpoint/lipsync/get-lipsync">
    track the progress of your video lip synchronization jobs.
  </Card>
</CardGroup>